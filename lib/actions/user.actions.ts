'use server';

export const signIn = async () => {
    try {
        //mutations / DB / Make fetch
    } catch (error) {
        console.error('Error',error)
    }
}

export const signUp = async () => {
    try {
        //mutations / DB / Make fetch
    } catch (error) {
        console.error('Error',error)
    }
}